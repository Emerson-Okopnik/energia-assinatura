# roles/nginx/handlers/main.yml
- name: Reload nginx
  # Executado quando templates ou links de sites mudam para aplicar CORS/logs sem interrupção
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
    daemon_reload: true